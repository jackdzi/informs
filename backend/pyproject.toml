[project]
name = "informs-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlmodel>=0.0.22",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
